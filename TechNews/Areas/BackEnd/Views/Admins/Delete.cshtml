@model TechNews.Areas.BackEnd.Models.Admin

@{
    ViewData["Title"] = "刪除管理員";
}

<div class="container-fluid">
    <!-- Page Heading -->
    <h1 class="h3 mb-4 text-gray-800">刪除管理員</h1>

    <!-- Description -->
    <p class="text-muted">
        您即將刪除以下管理員資料，該操作無法恢復。請確認是否要繼續。
    </p>

    <!-- Admin Details -->
    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-danger">管理員詳細資訊</h6>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3 font-weight-bold">帳號</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.AdminAccount)</dd>

                <dt class="col-sm-3 font-weight-bold">密碼</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.AdminPassword)</dd>

                <dt class="col-sm-3 font-weight-bold">名稱</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.AdminName)</dd>

                <dt class="col-sm-3 font-weight-bold">啟用狀態</dt>
                <dd class="col-sm-9">
                    @if (Model.IsActive)
                    {
                        <span class="badge badge-success">啟用</span>
                    }
                    else
                    {
                        <span class="badge badge-secondary">停用</span>
                    }
                </dd>

                <dt class="col-sm-3 font-weight-bold">最後登入時間</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.LastLogin)</dd>

                <dt class="col-sm-3 font-weight-bold">最後修改時間</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.ModifiedDate)</dd>

                <dt class="col-sm-3 font-weight-bold">群組</dt>
                <dd class="col-sm-9">@Html.DisplayFor(model => model.Group.GroupId)</dd>
            </dl>
        </div>
    </div>

    <!-- Actions -->
    <div class="d-flex justify-content-end mt-4">
        <form asp-action="Delete" class="mr-3">
            <input type="hidden" asp-for="AdminId" />
            <button type="submit" class="btn btn-danger">
                <i class="fas fa-trash-alt"></i> 確認刪除
            </button>
        </form>
        <a asp-action="Index" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> 返回列表
        </a>
    </div>
</div>
